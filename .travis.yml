language: php

php:
   - '5.6'

cache:
  directories:
    - $HOME/.composer/cache

before_script:
  - phpenv config-rm xdebug.ini
  - composer install --optimize-autoloader

script:
  - ./vendor/bin/phpcs --colors -p
